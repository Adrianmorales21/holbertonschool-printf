#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <string.h>
#include <stdarg.h>
#include <stdlib.h>
int print_string(char *c);
int call_functions(const char* format, ...);
int _putchar(char c);
int _printf(const char * format,...);

#endif		
