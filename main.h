#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <string.h>
#include <stdarg.h>
#include <stdlib.h>
int print_string(char *c);
int (*cf(char *s)(char));
int _putchar(char c);
int _printf(const char *format,...);



typedef struct fp
{	
char *fp;
int (*pt(char));
} fp_t;
#endif		
